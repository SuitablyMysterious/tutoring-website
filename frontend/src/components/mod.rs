//! Pure presentational components. They take props and render — they never
//! read `content::*` or theme values directly; pages wire those in.

pub mod footer;
pub mod hero;
pub mod nav;
pub mod section;
pub mod service_card;
